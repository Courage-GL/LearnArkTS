/**
 * 我的页面
 */
import router from '@ohos.router';
struct MyPage extends   {
    constructor(/**
     * 我的页面
     */ /**
 * 我的页面
 */
    /**
     * 我的页面
     */
    ) {
        /**
         * 我的页面
         */
    }
    loginOut() {
        router.back();
    }
    build() {
        ;
    }
}
struct SettingDataBind extends   {
    constructor(/**
     * 我的页面
     */ /**
 * 我的页面
 */
    /**
     * 我的页面
     */
    ) {
        /**
         * 我的页面
         */
    }
    build() {
            .width('95%')
            .justifyContent(FlexAlign.Start)
            .borderRadius(12)
            .height(50)
            .alignItems(VerticalAlign.Center)
            .margin({ left: 20, top: 10, right: 20 });
    }
}
class ItemData {
    constructor(icon, content) {
        this.icon = icon;
        this.content = content;
    }
}
//# sourceMappingURL=MyPage.js.map